# Copy to .env and fill in. Never commit the real .env.

# Gmail app password (requires 2FA on the Google account):
# myaccount.google.com -> Security -> 2-Step Verification -> App passwords
JOB_RADAR_SMTP_USER=
JOB_RADAR_SMTP_PASSWORD=

# Optional secondary source (developer.adzuna.com)
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
